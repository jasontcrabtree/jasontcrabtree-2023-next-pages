import ClientComponent from "./client-component";

export const metadata = {
    title: 'Worklog | Mark I',
    description: '',
};

const App = async () => {
    return (
        <ClientComponent />
    )
}

export default App;